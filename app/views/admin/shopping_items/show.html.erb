<section class="page-title">
  <p class="eyebrow">Admin</p>
  <h1><%= @shopping_item.name %></h1>
  <p><%= @shopping_item.user.email %></p>
</section>

<section class="card">
  <p><%= @shopping_item.manual? ? "手動追加" : "献立由来" %> / <%= @shopping_item.purchased? ? "購入済み" : "未購入" %></p>
</section>
<%= link_to "編集", edit_admin_shopping_item_path(@shopping_item), class: "secondary-button" %>
