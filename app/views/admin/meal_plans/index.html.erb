<section class="page-title">
  <p class="eyebrow">Admin</p>
  <h1>献立管理</h1>
</section>

<section class="card">
  <% if @meal_plans.any? %>
    <% @meal_plans.each do |plan| %>
      <div class="dev-link-row">
        <span><%= plan.user.email %> / <%= l(plan.meal_date, format: :long) %> / <%= meal_type_label(plan.meal_type) %></span>
        <%= link_to "詳細", admin_meal_plan_path(plan) %>
      </div>
    <% end %>
  <% else %>
    <p class="empty-state">献立はありません。</p>
  <% end %>
</section>
