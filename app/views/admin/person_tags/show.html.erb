<section class="page-title">
  <p class="eyebrow">Admin</p>
  <h1><%= @person_tag.name %></h1>
  <p><%= @person_tag.user.email %></p>
</section>

<section class="card"><p><%= @person_tag.default_selected? ? "初期選択あり" : "初期選択なし" %></p></section>
<%= link_to "編集", edit_admin_person_tag_path(@person_tag), class: "secondary-button" %>
